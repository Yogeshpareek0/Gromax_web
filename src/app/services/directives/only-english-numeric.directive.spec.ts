import { OnlyEnglishNumericDirective } from './only-english-numeric.directive';

describe('OnlyEnglishNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyEnglishNumericDirective();
    expect(directive).toBeTruthy();
  });
});
